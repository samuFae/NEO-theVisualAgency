import { graphWidth } from "@styles/shared/shared";
import styled from "styled-components/macro";

export const GraphWrapper = styled.div`
  grid-row: graph;
  width: ${graphWidth};
  height: 100%;
  position: relative;
`;
